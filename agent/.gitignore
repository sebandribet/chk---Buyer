node_modules/
.env
*.log
.DS_Store

# La interfaz web no va en este branch: se entrega el agente headless.
# Los archivos siguen existiendo localmente (npx tsx --env-file-if-exists=.env src/cli/server.ts).
src/cli/server.ts
src/cli/ui.html

# El enunciado del challenge es de los organizadores, no va en el repo.
*.docx
